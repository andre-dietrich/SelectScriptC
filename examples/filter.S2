#
#mean = PROC(x) : (SELECT $sum@+($)
#                    FROM $x
#              START WITH $sum = 0
#                      AS void) / len($x);

filter = PROC(x, f) : SELECT $f($) FROM $x;

#min = PROC(x, m) : SELECT if($ < $m, $m, $)
#                     FROM $x;

mean = PROC(x) : SELECT ($[-1]$ + $ + $[1]$) / 3.0
                   FROM $x
                  WHERE $[-1]$ AND $[1]$;
